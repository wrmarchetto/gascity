{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": [
    "schema_version",
    "ok",
    "running",
    "pid",
    "socket_path",
    "checked_paths"
  ],
  "properties": {
    "schema_version": {
      "type": "string"
    },
    "ok": {
      "const": true
    },
    "running": {
      "type": "boolean"
    },
    "pid": {
      "type": "integer"
    },
    "socket_path": {
      "type": "string"
    },
    "checked_paths": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "formula_source": {
      "type": "string",
      "enum": [
        "ref",
        "working-tree"
      ],
      "description": "Which formula source the running supervisor resolves from. ABSENT when the supervisor did not answer the formularef probe -- every build predating it is silent, and absence must not be read as \"working-tree\"."
    },
    "formula_ref": {
      "type": "string",
      "minLength": 1,
      "description": "The committed git ref formulas resolve from. Present only when formula_source is \"ref\"."
    }
  },
  "additionalProperties": true
}
